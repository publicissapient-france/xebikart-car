---

- name: "install os packages"
  apt:
    name: "python-setuptools"
    update_cache: "yes"
    state: "present"

- name: "install donkeycar package"
  pip:
    name: "donkeycar[pi]==2.5.8"
    virtualenv: "{{ user_directory }}/env"
