---

- name: "install os packages"
  apt:
    name:
      - python-setuptools
      - libopenmpi-dev
    update_cache: "yes"
    state: "present"

- name: "install python dependencies"
  pip:
    name: "mpi4py"
